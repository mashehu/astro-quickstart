---
import { Code } from 'astro/components';

export const prerender = false;

const content = await fetch("https://raw.githubusercontent.com/withastro/docs/main/tsconfig.json").then((res) => res.text());
---
<Code code={content} lang={'json'}/>
